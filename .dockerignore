# Keep the build context lean and reproducible: never ship local installs or
# build outputs into the image (CI rebuilds from source), and drop everything
# the Vite build doesn't read.
node_modules
dist
coverage
test-results
playwright-report
artifacts
.git
.github
.husky
agents
tests
scripts
.env*
.DS_Store
Dockerfile
.dockerignore
