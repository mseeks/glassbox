# Claude Code OAuth token — subscription auth, NOT an API key.
# Mint it with `claude setup-token`, then paste the value after the `=`.
# Keep ANTHROPIC_API_KEY unset so the Agent SDK uses this token instead.
CLAUDE_CODE_OAUTH_TOKEN=
